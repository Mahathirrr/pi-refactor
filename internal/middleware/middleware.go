// Package middleware berisi middleware aplikasi
package middleware

import (
	"github.com/gin-gonic/gin"
)

// Setup menginisialisasi semua middleware yang dibutuhkan
func Setup(r *gin.Engine) {
	// Tambahkan middleware di sini jika diperlukan
	// Contoh: r.Use(cors())
}